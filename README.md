# homebrew-tap

Homebrew tap for Interactor OSS tools (e.g. `ibuild`).

## Install

```sh
brew install interactor-oss/tap/build
```

This installs `ibuild`, the Interactor build sync daemon. Source: [InteractorOSS/product-manager](https://github.com/InteractorOSS/product-manager).
